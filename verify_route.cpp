/**
 * verify_route.cpp - Simple test to verify Route class methods
 */

#include "core/v1/route.h"
#include "core/v1/state.h"
#include "core/v1/board.h"

// This file is just to test compilation of Route class methods
void verifyRouteMethods()
{
    // This won't actually run, just tests compilation
    /*
    PBoard board;
    PState state(board, OrbLocation(0,0), OrbLocation(0,1), 0, 10);
    Route route(&state);
    
    int score = route.getScore();       // Test getScore()
    route.printFinalBoard();            // Test printFinalBoard()
    route.printRoute();                 // Test existing method
    */
}